import { TareaStatusPipe } from "./tarea-status-pipe.pipe";

describe('Pipe: TareaStatusPipee', () => {
  it('create an instance', () => {
    let pipe = new TareaStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
